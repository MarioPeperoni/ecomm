import { LoginCard } from "@/components/auth/LoginCard";

export default function AdminAuthPage() {
  return (
    <main className="flex h-screen flex-col items-center justify-center bg-primary/60">
      <LoginCard />
    </main>
  );
}
